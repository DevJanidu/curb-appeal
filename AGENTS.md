# Curb Appeal

Use the skills in `skills/` when designing or implementing a local-business website. Start with `website-director`, then load only the relevant supporting skills. Preserve the repository's existing framework and conventions unless the user explicitly requests a migration.
